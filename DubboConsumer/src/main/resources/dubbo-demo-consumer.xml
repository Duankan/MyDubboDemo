<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
       http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 消费方应用名称，用于计算依赖关系，不是匹配条件，不要与提供方名称一样  -->
    <dubbo:application name="demo-consumer"/>

    <!-- 使用zookeeper注册中心暴露服务地址  这个很关键，弄了几天这个影响极大 -->
    <dubbo:registry protocol="zookeeper" address="127.0.0.1:2181"/>
    <!-- 使用dubbo协议，在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20881"/>
    <!--<dubbo:consumer check="false" />-->
    <!-- 生成远程服务代理，可以与本地bean一样使用  check属性，启动时候是否检查 一般设置成false 启动时候不检查 -->
    <dubbo:reference id="Mydemo"  interface="com.github.Duankan.service.IDubboService" timeout="1200000"/>
    <dubbo:reference id="IUsers"  interface="com.github.Duankan.service.IUsers" timeout="1200000"/>
    <dubbo:reference id="IRole"  interface="com.github.Duankan.service.IRole" timeout="1200000"/>
    <dubbo:reference id="IPermission"  interface="com.github.Duankan.service.IPermission" timeout="1200000"/>
</beans>